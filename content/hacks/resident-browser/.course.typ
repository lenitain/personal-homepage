// 课件共用的一点小东西。被各章 #import，自己不以文件名出现在文件树里（点号开头）。
//
// 这里**只放排版必需的东西**，不放「提示框」之类的装饰。原因见下面 title 的注释：
// 这套课件的结构全部由标题承载 —— 一个问题一个标题，一个实验一个标题 ——
// 而不是由带颜色的框承载。框会把注意力吸到框上，标题不会。

/// 章标题。
///
/// typst 的 HTML 导出把 `=` 映射成 `<h2>` —— 它给「文档标题」留出了 h1。
/// 而本课程每一章本身就是一份文档，标题理应是最外层那个（也才拿得到站点里
/// 绿色下划线那条样式，跟 readme 的观感一致）。所以章标题不用 `=`，用这个。
#let title(body) = html.elem("h1", body)

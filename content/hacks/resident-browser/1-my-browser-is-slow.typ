#import ".course.typ": title, ask, lab, oops, note, punch, cols

#set document(title: "我的 qutebrowser 启动好慢，我该怎么办？")

#title[我的 qutebrowser 启动好慢，我该怎么办？]

qutebrowser 启动要 1.2 秒。一天开几十次，每次都等这 1.2 秒。

*该从哪里入手呢？*

= 1. 让我们看看一个进程的启动过程吧

== 「启动」是什么意思

在动手之前，得先弄清楚一个更基础的问题：
*在 Linux 上「跑一个程序」到底是什么意思？*

现存所有的 Unix 及类 Unix 操作系统内核（Linux、macOS、BSD、Plan 9 …），
都用相同的方式创建进程。进程创建的过程是后面所有事情的入口。

=== 它不是一步，是两步

如果你没想过这件事，直觉大概是这样：敲一个命令，系统就新建一个进程去跑它。一步。

实际是两步：*先复制一个现有的进程，再把复制品的内容换掉。*

第一步叫 `fork`，第二步叫 `execve`。名字不用记，看它们做什么就行。

=== 第一步：复制

#lab("演示 01：fork 把一个进程变成两个")[
  ```c
  printf("fork 之前：屏幕前只有我一个进程，pid = %d\n", getpid());
  pid_t pid = fork();          /* 这一行之后，代码在两个进程里各往下走一遍 */

  if (pid == 0)  printf("  [子进程] 我是复制出来的那一份，pid = %d\n", getpid());
  else           printf("  [父进程] 我还在，pid = %d；fork 交给我的那个 pid 是 %d\n",
                        getpid(), pid);
  sleep(1000000);              /* 停在这儿，等你在进程列表里看够了再收 */
  ```

  这个脚本不打印结论 —— 它让父子两个进程都停在那儿不走，
  你自己去进程列表里数：

  ```sh
  $ ./01-fork.sh
    …
        fork 之前：屏幕前只有我一个进程，pid = 184711
          [父进程] 我还在，pid = 184711；fork 交给我的那个 pid 是 184713
          [子进程] 我是复制出来的那一份，pid = 184713

        ┌ 打开 btop（或者 pstree -p、pgrep -a forkhold）找 forkhold：
          几个进程？两个 pid 差多少？
        └ 想好了按回车
  ```

  `fork` 只调用了一次，但它下面的代码*两个进程各跑了一遍*。
  两个进程唯一的区别是 pid —— 而 pid 就是通过 `fork` 的返回值告诉你的：
  子进程拿到 0，父进程拿到子进程的 pid。

  #note[
    这个实验有个不做就白做的细节：父子两边跑完*不能退出*。
    它们要是打完两行就结束了，你打开监视器的时候进程列表里什么都没有。
    脚本里还有一处：`fork` 出来的那个孩子要跟着父进程一起死
    （`prctl(PR_SET_PDEATHSIG, …)`）—— 不然外面收尾时只杀得掉父进程，
    孩子会一直赖在那儿。
  ]

  （完整脚本：`./docs/labs/resident-browser/01-fork.sh`）
]

=== 第二步：换内容

复制出来的那份，内容跟原来一模一样 —— 它还是 shell。所以还有第二步：
把这个进程的内容，换成你真正想跑的那个程序。

#lab("演示 02：execve 换掉内容，但不换进程")[
  这个脚本把进程*卡在 `execve` 之前*：先让你在进程列表里认出它、记住它的 pid，
  再放行 —— 你亲眼看着同一个 pid 变成另一个程序。

  ```sh
  $ ./02-execve.sh
          [换之前] pid = 184757   内核眼里的名字 = execstep

        ┌ 打开 btop，按 pid 184757 找到它（或者 cat /proc/184757/comm）：
          它现在叫什么名字？
        └ 想好了按回车

    放行 ——

          [换之前] pid = 184757   内核眼里的名字 = execstep
          [还是它] pid = 184757   内核眼里的名字 = execstep
          [换之后] pid = 184757   内核眼里的名字 = sh
  ```

  *两个 pid 一样。* 这就是关键：`execve` 不产生新进程，
  它只是把同一个进程里装的东西换掉。pid 不变，进程在系统里的身份也不变。

  「名字」那一栏是从 `/proc/<pid>/comm` 读的 —— 那是*内核眼里*这个进程叫什么，
  不是程序自己打印的字符串。换掉的正是这个。

  #note[
    卡住这一下必须由外面控制（脚本里用一根 FIFO），不能让它自己跑完 ——
    否则等你打开监视器，进程早就换完了，什么也看不到。
    这正是「实验的节奏要由观察者掌握」的一个例子。
  ]

  （完整脚本：`./docs/labs/resident-browser/02-execve.sh`）
]

=== 为什么要分成两步

一个很自然的问题是：既然要的是「跑一个新程序」，为什么不一步到位？

因为*中间那一步有用*：`fork` 和 `execve` 之间，你可以先把这个进程的环境布置好，
再让它变成别的程序。shell 的重定向和管道都靠这一步 —— 你敲 `echo hello > out.txt`，
echo 根本不知道有文件这回事，是 shell 在 `exec` 之前把子进程的标准输出改成了那个文件。

（完整脚本：`./docs/labs/resident-browser/03-redirect.sh`）

=== 回到我们的问题

你在 shell 里敲下 `qutebrowser` 回车，shell 做的事就是：

```sh
fork()     复制一个自己出来
execve()   把复制品的内容换成 qutebrowser
```

所以「启动一个程序」不是一个动作，是两个。而慢的那一个显然是第二步 ——
`fork` 只是复制一下，很便宜（后面会看到它便宜到什么程度）。

那么 `execve` 到底要花多少工夫？为什么换个程序会有快有慢？

要看清楚它，得先知道一个进程「里面」是什么样子。

== 一个进程里面有什么

一个运行中的进程，有一片属于它自己的内存。这片内存叫*地址空间*。

从进程自己看，这是*一整片连续*的地址：从 0 一直排到一个很大的数。数组里的元素
一个挨着一个，指针加上几就能走到第几个 —— 整个程序都建立在这个假设上。

物理内存本身也是一整条连续的 —— 从 0 一直排到顶。区别在分配方式：它被切成了
许多小块，用虚拟内存来管理。进程那一片连续的地址，是从这些小块里拼出来的：
相邻的两个地址，落到物理上可能隔着很远。

所以进程手里的每个地址都只是一串*编号*，每次访问都要先翻译一次，才知道落在哪一小块上。
你的进程里有 `0x400000`，隔壁那个进程里也有 `0x400000`：
同一个编号，落在两块毫不相干的物理内存上。

这不是硬件天生如此，是一次刻意的工程设计。为什么值得多这一层翻译，这里先不展开。

一个编号指向什么，有三种情况：

+   *只有你在用* —— 你刚写进去的变量，刚压进去的栈
+   *别人也在用* —— `libc` 的代码，全系统几十个进程共用同一份
+   *谁都还没指向* —— 映射了，但还没碰过；碰的那一刻才去磁盘上取

=== 那片「内存」是虚拟的

先量一下这三种情况加起来是什么规模：

#lab("演示 04：地址空间加起来，比物理内存大得多")[
  这个实验要两个数，第一个你自己在监视器里看：

  ```sh
  $ ./04-address-space.sh

        ┌ 打开 btop（或者 free -h）：这台机器的物理内存是多少？现在用了多少？
        └ 想好了按回车

    现在把机器上所有进程的地址空间加起来。
    这条命令不需要任何工具，你可以自己粘：

      $ awk '/^VmSize:/{s+=$2} END{printf "%.0f GiB\n", s/1048576}' /proc/[0-9]*/status

    实跑结果：

        所有读得到的进程，地址空间加起来   6573 GiB
        这台机器的物理内存                 15 GiB
        倍数                               438 倍

    其中最大的几个：

            1448 GiB  chromium
            1448 GiB  chromium
            1448 GiB  chromium

        ┌ 跟刚才 btop 里那个物理内存比一下：地址空间加起来是它的多少倍？
          再回头看最大的那几个 —— 一个进程怎么可能占得下？
        └ 想好了按回车
  ```

  每个进程的地址空间里都有 `libc`、都有栈、都从差不多的地方开始 ——
  如果这些真的是内存，这台机器早就炸了。

  要是这些编号都算内存，这台机器连一个浏览器都开不起来。

  别的用户的进程读不到，所以上面那个总和还是个下限。

  （完整脚本：`./docs/labs/resident-browser/04-address-space.sh`）
]

=== 映射不是占用

那「映射」又是什么？拿一个 1 GiB 的文件来试：

#lab("演示 05：映射 1 GiB，物理内存一点没涨")[
  一个进程「占多少内存」有两个完全不同的说法，这个实验把它们分开看：

  +   *地址空间* —— 它向内核要下来的地址范围有多大（`status` 里的 `VmSize`）
  +   *物理内存* —— 此刻真有多少页内容躺在内存里（`status` 里的 `VmRSS`）

  同一个进程，先「映射」一个 1 GiB 的文件，再「读」其中一部分。
  两件事听起来差不多，但只有一件事会让第二个数动：

  ```sh
  $ ./05-mmap-not-occupied.sh

    [1/2] 映射一个 1 GiB 的文件 —— 只是映射，一个字节都不读。

        ┌ 现在去看那个进程（它就停在那儿）：
          在 btop 里找 usemap，看它报多少内存；或者直接读它自己报的两个数：
            grep -E 'VmSize|VmRSS' /proc/<pid>/status
        └ 想好了按回车

    [2/2] 放行 —— 让它把其中前 256 MiB 挨页读一遍。

        ┌ 再看同一个进程的那两个数：哪一个涨了？涨了多少？
        └ 想好了按回车

  收工。把三次读数摆在一起（都是那个进程自己报的，没有加工）：

    起点            地址空间       3.1 MiB    物理内存       1.8 MiB
    mmap 1024 MiB   地址空间    1027.1 MiB    物理内存       1.9 MiB
    读过 256 MiB    地址空间    1027.1 MiB    物理内存     258.0 MiB
  ```

  看第一列到第二列：*地址空间*涨了 1024 MiB，*物理内存*基本没动。

  因为「映射」不是「读进来」。它只在页表里写下一句对应关系：
  *这一段地址，内容去磁盘上那个文件的这一段取*。写这句话不需要读文件，
  也不需要分配内存 —— 所以地址空间涨了，物理内存没动。

  真正把内容取进来，是程序*碰到*那些地址的时候：CPU 发现这一页还没有对应的
  物理内存，触发一次缺页（内核管这个叫*缺页*），内核这才去磁盘读一页、挂上去。
  碰多少页，涨多少页。

  内核装载一个可执行文件，用的就是这套动作：把 `PT_LOAD` 一段段*登记*进地址空间，
  然后走人。所以「`execve` 把文件搬进地址空间」这句话是不准确的 ——
  准确的说法是*映射*：只登记，不搬运。

  （完整脚本：`./docs/labs/resident-browser/05-mmap-not-occupied.sh`）
]

=== 同一份内容，可以只占一份物理内存

同一批物理页还可以同时属于很多个进程 —— 而且可以同时属于很多个。

#lab("演示 06：一百个进程，各自加载同一个 2 GiB 的库")[
  这个实验*不打印结论*。它分两步，每步做完停下来，让你自己打开监视器看内存 ——
  数字要你自己看见才算数：

  ```sh
  $ ./06-one-copy.sh

    这个实验分两步。每步做完会停下来 —— 你自己打开监视器看内存，
    看完按回车走下一步。

    看什么：btop（或者 gnome-system-monitor、free -h）的内存那一栏，
    以及进程列表里那几个 usebig 进程各自报多少。

        ┌ 先看一眼基线：内存现在用了多少？缓存多少？记住这两个数。
        └ 看完按回车

    [1/2] 起 1 个进程，让它把一个 2048 MiB 的共享库从头读到尾。

        ┌ 现在看一眼 btop。跟基线比涨了多少？进程列表里有几个 usebig，各报多少？
        └ 看完按回车

    [2/2] 再起 99 个一样的进程，各自也把那 2048 MiB 读完。

        ┌ 再看一眼 btop。整机又涨了多少？那 100 个进程各报多少？
          要是真的一人一份，这里得 200 GiB —— 这台机器装不下。
        └ 看完按回车
  ```

  看完那两眼，你手上会有这么两组数：

  ```sh
    基线         整机已用 5.9 GiB   缓存 6.6 GiB
    1 个进程     整机已用 5.9 GiB   缓存 8.6 GiB     进程列表 1 个，报 2.0 GiB
    100 个进程   整机已用 6.4 GiB   缓存 8.6 GiB     进程列表 100 个，各报 2.0 GiB
  ```

  缓存从 6.6 涨到 8.6 的那 2.0 GiB，是库*被读进来了* —— 一个进程读完，
  它整个进了页缓存。然后再加 99 个进程各自读完，缓存一点没涨。

  可 btop 的进程列表里，那 100 个 usebig 各自都报着 2.0 GiB —— 加起来 200.2 GiB。
  同一批物理页，被 100 个进程各映射了一次。

  #note[
    有两个细节不做，这个实验什么都看不出来，脚本里都留着注释：
    进程读完*不能退出*（退出了你还看什么）；
    库*不能放在 `/tmp`* —— 那台机器的 `/tmp` 是 tmpfs，文件本身就是内存，
    从建出来那一刻就占着 RAM，「读一遍」不会让内存有任何变化。
    脚本因此把工作目录开在 `$HOME/.cache` 下，并且在开演前先把库
    从页缓存里赶出去（`sync` + `posix_fadvise(DONTNEED)`），
    否则刚写完的库还在内存里，第一步就没得看。
  ]

  回头看 `fork`：它复制的是这一堆*约定*，不是约定指向的物理页。
  复制完两边指向同一批页，谁要写，谁才拿到自己的一份 ——
  这就是上一节说「`fork` 很便宜」的全部原因。

  （完整脚本：`./docs/labs/resident-browser/06-one-copy.sh`）
]

=== 这些约定排成一列区间

地址空间不是一整块，而是这样一堆区间拼起来的。Linux 把它们一行行列出来给你看：

```sh
$ cat /proc/self/maps
00400000-00401000 r--p 00000000  …/hello-static
00401000-0048c000 r-xp 00001000  …/hello-static
004c6000-004c8000 rw-p 000c6000  …/hello-static
004c8000-004ce000 rw-p 00000000  [heap]
```

每一行是一个区间：起止地址、权限、以及它对应哪个文件 —— 最后一列就是上面说的那句
「约定」。那一列是空的，说明这段内容不在任何文件里（栈、堆就是这样）。

现在有了「地址空间」这个概念，就可以回答上一节留下的问题了。

== execve 拿到一个文件，怎么把它变成地址空间

`execve` 的输入是一个可执行文件。它要做的，是把这个文件的内容，
按照某种安排放进新建的地址空间里。

安排写在哪里？写在文件自己里面。这类文件叫 *ELF*，它开头有一张表，
每一项描述「我有一段东西，请放到地址空间的某个位置，权限是这样」：

#table(
  columns: (auto, 1fr),
  table.header([表项], [含义]),
  [`PT_LOAD`], [要放进地址空间的一段，自带位置和权限],
  [`PT_INTERP`], [这个文件自己跑不起来，需要先请一个解释器（下一节讲）],
)

内核照着 `PT_LOAD` 一项一项地放。放完之后，地址空间就成形了 ——
表里的每一项，都能在 `/proc/self/maps` 里找到对应的那一行：
起始地址一样、长度一样、权限一样。

（这一一对应是可以逐条断言的，脚本在 `./docs/labs/resident-browser/07-elf-loading.sh`。）

到这里，一个「自足的」程序就起来了：文件里什么都有，内核照着登记完就能跑。

但现代程序大多不是自足的。

== 很多程序自己跑不起来

你写一个程序，要用到「打印到屏幕」这个功能。这个功能不是你写的，是别人写好的，
放在一个叫「库」的文件里。你的程序只写了「调用它」，没有把它的代码抄进来。

于是你的可执行文件里就缺了一块 —— 它自己跑不起来。

怎么办？在那张表里加一项，写上「我需要一个解释器」：

```sh
$ readelf -lW hello-static | grep -A1 INTERP
（没有这一项，这个文件是自足的）

$ readelf -lW hello-dyn | grep -A1 INTERP
  INTERP  …
      [Requesting program interpreter: /lib64/ld-linux-x86-64.so.2]
```

内核看到这一项之后，行为变了一点：它把地址空间建好之后，*跳转的不是这个程序，
而是那个解释器*；原程序只是作为参数被交出去，请解释器把它跑起来。

那个解释器叫*动态链接器*。它的任务就是补上缺的那一块。

== 解释器做什么

它的工作可以拆成四件事：

+   *找库* —— 你的程序说「我要用 libc」，它得去磁盘上把 libc 找出来
+   *把库映射进地址空间* —— 跟内核做的事一样，只不过这次是给库做的
+   *修正地址* —— 库被放在哪是随机的，代码里写死的那些地址得重算一遍（叫*重定位*）
+   *调用各家的初始化函数* —— 每个库都可能有「用之前得先准备好」的东西

这四件事都要花时间，而且每一件都是*每次启动重来一遍*。glibc 自带开关，能把它们打出来：

#lab("演示 08：把解释器的工作打出来看")[
  这个脚本每一节都先把输出挡住，让读者猜一下再放出来 ——
  四件事里有三件的答案跟直觉不一样：

  ```sh
  $ ./08-dynamic-loader.sh

    ┌ 第一节，找库。这个库就放在当前目录，链接器还得去找。先猜一下：
      它要试几个地方，才轮到我们放的那个？
    └ 想好了按回车

  放出来看 —— 它找 libctor.so 的完整过程：

          trying file=/tmp/tmp.T2pF6iHTV3/glibc-hwcaps/x86-64-v3/libctor.so
          trying file=/tmp/tmp.T2pF6iHTV3/glibc-hwcaps/x86-64-v2/libctor.so
          trying file=/tmp/tmp.T2pF6iHTV3/libctor.so

  试了 3 个地方，才轮到我们那个。前几个是 glibc-hwcaps：
  按 CPU 支持的指令集档次逐级回退，找不到再退回普通路径。

    ┌ 第二节，初始化顺序。你自己的 main 和它依赖的库，谁的「准备好」先跑？
    └ 想好了按回车

        calling init: /lib64/ld-linux-x86-64.so.2
        calling init: /usr/lib/libc.so.6
        calling init: /tmp/tmp.T2pF6iHTV3/libctor.so
        initialize program: ./ctor-demo

  你的 `main` 不是第一个跑的东西 —— 它依赖的所有东西都必须先就绪。

    ┌ 第三节，账单。找库、映射、重定位、调 init，先猜哪一件最贵？
    └ 想好了按回车

          total startup time in dynamic loader: 62582 cycles
                    time needed for relocation: 1560 cycles (2.4%)
                         number of relocations: 101
                   time needed to load objects: 19863 cycles (31.7%)
  ```

  三件事各自对应解释器的一项工作：

  +   *找库* —— 不是查一次表，是按层级逐级回退着试（那三行 `trying file=`）
  +   *调 init* —— 按依赖顺序来，你自己的 `main` 排最后
  +   *映射* —— 账单里最贵的那一项，但它只是 mmap 登记（见演示 05），
      不是把内容读进内存

  #note[
    「找」这件事后面还会再遇到：不存在的路径照样要发一次查找、拿到一个
    「没有这个文件」，才轮到下一个。这类细碎的文件操作是启动时间里真实的一笔 ——
    体检四会把这笔账完整量出来。
  ]

  （完整脚本：`./docs/labs/resident-browser/08-dynamic-loader.sh`）
]

== 这套流程的代价是多少

上面那一整套流程要花多少时间？先看一个最小的例子：同一个 `hello.c` 编两遍，
一个是静态链接（自足，前面「execve 拿到一个文件」那节的路），
一个要用解释器（后面两节的路）—— 两个程序都只打印一行字，做的事一模一样。
各跑 400 次，取中位数：

#cols[
  *自足的（静态）*
  ```sh
  357 µs
  （这里面绝大部分是
    fork + execve 本身）
  ```
][
  *要用解释器的（动态）*
  ```sh
  513 µs
  （多了 156 µs）
  ```
]

#punch[
  那多出来的 156 µs，就是第 4、5 节里那一整套：
  启动解释器、找库、把库登记进地址空间、重定位、调初始化。
]

而且注意：这 156 µs 跟程序要做什么*毫无关系*。这两个程序都只打印一行字，
但该找的库一个都不能少找，该重定位的一个都不能少算。

一个程序越复杂，依赖的库越多，这一段就越长。qutebrowser 依赖 Qt、QtWebEngine、
Python 解释器和它的一大堆模块 —— 1.2 秒里，很大一块是这个。

（这两个数、以及上面那四件事的原始输出，都在演示 07 的脚本里：
`./docs/labs/resident-browser/07-elf-loading.sh` —— 它绑核跑 400 次取中位数。）

（那这些库真的被读进内存了吗？没有 —— 第 2 节量过：映射只是登记，
碰到哪一页才读哪一页。所以这里贵的是*找*和*算*，不是*读*。）

= 2. 用这套机制给 qutebrowser 做体检

现在手里有工具了。回到最初的问题：那 1.2 秒花在哪。

体检的对象就是*普通的 qutebrowser* —— 从 shell 里敲的那个命令，不带任何修饰。
全部只用外部观测，不动它一行代码。

== 体检一：它到底是什么

```sh
$ file /usr/bin/qutebrowser
/usr/bin/qutebrowser: Python script, ASCII text executable
$ ls -l /usr/bin/qutebrowser
-rwxr-xr-x 1 root root 970  4月  4 00:40 /usr/bin/qutebrowser
$ head -2 /usr/bin/qutebrowser
#!/usr/bin/python3
# EASY-INSTALL-ENTRY-SCRIPT: 'qutebrowser==3.7.0','gui_scripts','qutebrowser'
```

*它不是一个二进制，是一个 970 字节的 Python 脚本。*

这件事直接接上第 1 节：shell 那次 `execve` 交出去的文件，内核没法直接执行 ——
它是一个 shebang 脚本。内核的处理方式和处理 `PT_INTERP` 是同一个思路：
*不跑这个文件，去找它指定的那个程序来跑*。只不过这次要找的不是动态链接器，
而是 `/usr/bin/python3`。

== 体检二：这一路上 exec 了什么

```sh
$ PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin \
    strace -f -e trace=execve qutebrowser --version
```

去重之后，真正 exec 成功的程序只有四个：`qutebrowser`（脚本自身）、
`ldconfig`、`uname`、`file`。另外还有两个*不是 exec 出来的*：

```sh
execve("/usr/lib/qt6/QtWebEngineProcess", ["--type=zygote", "--no-zygote-sandbox", …])
execve("/usr/lib/qt6/QtWebEngineProcess", ["--type=zygote", …])
```

连 `qutebrowser --version` 这种「什么都不干」的调用，QtWebEngine 都会先起两个
zygote 模板进程。第 1 节讲的「fork 继承地址空间」在这里第一次露出它为什么重要。

#note[
  ⚠️ 这个体检里有个*方法论陷阱*，第一次量的时候我就被骗了。

  不清 `PATH` 直接跑，trace 里会出现*几十条* `execve` —— 全都是在我的 mise 配置里
  逐个 install 目录试探 `uname` / `file` 的记录，跟 qutebrowser 毫无关系。

  也就是说：*测量环境本身也是变量。* 在把这类噪声清干净之前，
  任何关于「启动花了多少时间」的数字都不可信。

  还有一处我没查清：清掉 `PATH` 之后 `uname` / `file` / `ldconfig` 仍然出现，
  而 qutebrowser 自己不该调用它们。来源没查明，先记在这里。
]

== 体检三：Python 侧的账单

qutebrowser 的入口是 Python，所以先量它导入了什么。Python 自带一个开关：

```sh
$ python3 -X importtime -c "import qutebrowser.qutebrowser"
```

累计耗时最长的几项：

#table(
  columns: (auto, auto, 1fr),
  table.header([累计], [自身], [模块]),
  [90.4 ms], [0.9 ms], [`qutebrowser.qutebrowser`],
  [62.9 ms], [0.9 ms], [`qutebrowser.misc.earlyinit`],
  [38.9 ms], [2.2 ms], [`traceback`],
  [28.2 ms], [*10.4 ms*], [`_colorize`],
  [17.8 ms], [1.5 ms], [`dataclasses`],
  [17.5 ms], [0.4 ms], [`json`],
  [14.4 ms], [5.7 ms], [`inspect`],
  [13.6 ms], [1.2 ms], [`re`],
)

*一共 109 个模块，光是把它们导进来就要 90 毫秒。* 而这里最费时的一项
（`_colorize`，Python 3.14 给报错信息上色的模块）跟浏览器本身毫无关系 ——
它只是被 `traceback` 顺带拖进来的。

（完整脚本：`./docs/labs/resident-browser/13-qutebrowser-checkup.sh`）


== 体检四：把那 1.2 秒切成几段

前面三步是分项看。这一节把它放回时间线上：给启动全程打上时间戳，
然后找几个*可以外部观测*的界标。

```sh
$ strace -f -tt -o trace.txt qutebrowser -R about:blank
```

三个界标（时刻取自一次真实运行）：

#table(
  columns: (auto, auto, 1fr),
  table.header([时刻], [相对], [事件]),
  [02:36:43.113], [0], [`execve("/usr/bin/qutebrowser")` —— 起点],
  [02:36:43.346], [*+233 ms*], [第一个 `libQt6*.so` 被打开 —— Python 阶段结束],
  [02:36:44.360], [*+1247 ms*], [第一次 connect 到 wayland —— 窗口要出现了],
)

于是这 1.25 秒大致分成两段：*前 233 毫秒是 Python*，*后 1014 毫秒是 Qt 和 QtWebEngine*。
跟体检三的 90 ms import 账单对得上 —— Python 那一侧的量级是对的。

=== 那 1014 毫秒具体在干什么

把这一段（+0.25s 到 +1.30s）里打开的路径按次数排出来：

```sh
  qutebrowser/qt/webkit.py  (+ .pyc)                   ← 导入 QtWebEngine 模块
  /usr/lib/qt6/plugins/platforms/libqwayland.so       ← Qt 装载平台后端
  /usr/lib/qt6/plugins/wayland-shell-integration/libxdg-shell.so
  /usr/lib/libOpenGL.so.0   libGLdispatch.so.0
  /usr/lib/libharfbuzz.so.0 libfreetype.so.6  libpng16.so.16  libgraphite2.so.3
  /usr/lib/libdbus-1.so.3   libxcb.so.1  libXau.so.6  libXdmcp.so.6
  /dev/shm/.org.chromium.Chromium.*                   ← Chromium 建共享内存段
```

同一段时间窗里的系统调用画像：

#table(
  columns: (auto, auto, 1fr),
  table.header([系统调用], [次数], [说明]),
  [`openat`], [932], [打开文件],
  [`read`], [1508], [读内容],
  [`newfstatat`], [1304], [问「这个文件在不在、多大」],
  [`mmap`], [1007], [映射进地址空间],
  [`fstat` / `lseek` / `close`], [875 / 630 / 916], [读完收尾],
  [`clock_gettime`], [2496], [各处计时],
)

*这是一段大量的小文件操作，不是在算东西。* 顺序上很清楚：
先导入 QtWebEngine 的 Python 模块，然后 Qt 装载平台后端和一堆本地库，
最后 Chromium 建立自己的共享内存。这三件事串起来就是那 1014 毫秒。

#note[
  一处没查清的地方，记在这里：体检二里连 `qutebrowser --version` 都会 exec 出两个
  `QtWebEngineProcess`，但这次 trace 里那个进程一次都没出现。`strace -f` 会跟随所有
  fork，所以不是漏抓。差别在哪还不知道 —— 这里不猜。
]

== 体检到此为止

细节还能再往下拆（adblock 规则解析、profile 装载、会话恢复各占多少），
但主要开销已经清楚了，不必再往下钻：

#punch[
  1.25 秒里，约 90 毫秒是 Python，约 1 秒是 Qt 和 QtWebEngine 的初始化。
]

还有一件事值得注意，它是这次体检里最有用的一个观察：

为了量这一秒，上面用的命令是 `qutebrowser -R about:blank` ——
*开的是一个空白页。* 而这一秒照样花了。

也就是说，这一秒发生在「你要求它打开什么」之前。它跟你要访问的网页没有关系，
跟你有几个标签页也没有关系 —— 只要启动，就得付。

#note[
  那剩下的 adblock、profile、会话恢复呢？它们当然也要花时间，但它们是*跟着你打开
  什么*走的：会话里有二十个标签页就多花一点，没有就少花一点。
  那是另一类开销，不属于这里说的这一秒。
]


= 3. 小结：现在知道什么，还不知道什么

*已经知道的：*

+   Linux 弄出一个新进程是两步：`fork` 复制一份，`execve` 把内容换掉
+   分成两步是为了留出中间那一步 —— shell 的重定向和管道都靠它
+   而 `fork` 之所以便宜，是因为地址空间是*虚拟*的：复制的是约定，不是物理页
+   地址空间里的地址是一串编号：映射不等于占用，同一份内容可以只占一份物理内存
+   这一列约定就是 `/proc/self/maps` 里的那些区间
+   可执行文件里有一张表，说清楚「哪段内容登记到哪儿、什么权限」，内核照着登记
+   如果文件里写了「我需要解释器」，内核启动的其实是解释器，原程序交给它
+   解释器要做四件事：找库、登记进来、修正地址、调初始化
+   这一整套每次启动都要付，而且跟程序要做什么无关

*关于 qutebrowser：*

+   它不是一个二进制，是一个 970 字节的 Python 脚本
+   光把 Python 模块导进来就要 90 毫秒
+   从敲命令到窗口要出现一共 1.25 秒，切成两段：
    *前 233 毫秒是 Python，后 1014 毫秒是 Qt 和 QtWebEngine*
+   后面那 1014 毫秒做的事很具体：导入 QtWebEngine 模块 → Qt 装载平台后端 →
    一大堆本地库被映射进来 → Chromium 建立共享内存。
    全是*文件操作*，不是在算东西

*还有一个最关键的观察：*

+   上面这一秒是用 `-R about:blank` 量的 —— 开的是空白页，它照样花了
+   所以*这一秒跟你要打开什么无关*。只要启动就得付，开一个标签页和开五十个，
+   这一秒是一样的

#punch[
  一笔每次都一样、而且跟你要做的事无关的开销 ——
  这正是「能不能只付一次」这个问题的起点。
]

这件事是下一章要做的。

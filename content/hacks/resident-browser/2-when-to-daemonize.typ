#import "../../.course.typ": title, ask, lab, note, oops, punch
#import ".syllabus.typ": chapterRef

#set document(title: "什么样的程序值得常驻")

#title[什么样的程序值得常驻]

每一篇讲「怎么让慢程序变快」的文章，最后都会给同一个建议：启动一次，让它一直跑着，
用 socket 跟它说话。

这个办法管用。但它通常是个*错误的默认选择*，因为它被当成*提速技巧*推销，
而它实际上是一笔*内存交易*：你并没有把启动开销删掉，你是把它一次付清，
然后为那个结果永久付租金。

划不划算，取决于一件比「这个程序慢不慢」具体得多的事。

= 1. 同一个思路的四次出现

「常驻进程 + 轻客户端」不是新发明，它有一条很长的历史：

-   `inetd`（4.3BSD 时代）按需拉起网络服务，然后把连接交给它
-   Emacs server：编辑器的启动太贵，于是常驻一个，用 `emacsclient` 送文件过去
-   systemd 的 socket activation：监听 socket 的活先由 init 拿着，真有连接了再把服务拉起来
-   今天的 Ghostty、qutebrowser：一模一样，只是把管道换成了 D-Bus 或 Unix socket

一条四十年的思路，反复出现在完全不同的软件里。*这说明它背后有个稳定的判据*，
而不是某种潮流。找那个判据，比记住某个具体实现有用得多。

= 2. 判据：慢的那部分，能不能跟你的请求拆开

不是*它有多慢*，而是：

#punch[
  存不存在一大块工作，它在程序知道你想要什么*之前*就做完了，
  而且*每一次都产出完全相同的结果*？
]

== 浏览器：有

Python 解释器、Qt、Chromium 引擎、adblock 规则、profile。不管你接下来要开一个标签页
还是五十个，这些完全一样。它们是可以保温的*前言*。

体检给出的量：一个「什么都不要求」的请求 —— 打开空白页 —— 也要先付掉这些。

#lab("演示 13：打开一个空白页，前言要付多少")[
  ```sh
  给启动全程打时间戳（strace -f -tt /usr/sbin/qutebrowser -R about:blank），三个外部可观测的界标：
    15:03:52.096630      0 ms  execve("/usr/sbin/qutebrowser") —— 起点
    15:03:52.433206   +337 ms  第一个 libQt6Core.so.6 被打开 —— Python 阶段结束
    15:03:53.905270  +1809 ms  第一次 connect 到 wayland —— 窗口要出现了
  ```

  `about:blank` 是最省事的请求：没有页面、没有网络、没有脚本。它照样要付 1.8 秒 ——
  这 1.8 秒不是为这个页面花的。

  #note[这一段全程在 `strace` 下跑，跟踪本身有开销，绝对值比平时慢。要看的是两段的比例和量级。]

  （完整脚本：`./docs/labs/resident-browser/13-qutebrowser-checkup.sh`）
]

== 编译器：没有

`gcc` 的启动本身是个舍入误差：

```sh
$ gcc --version          # 什么都不编译：1.4 ms
$ gcc -c tiny.c          # 编译一个空 main：15 ms
```

（30 次取最小值，绑核无关 —— 这两个数量级差十倍以上，不用那么讲究。）

时间花在读*你的*源码、产出*你的*二进制上，而这些工作在你开口说「编译什么」之前
根本没法开始。所以没有东西可以保温，因为*没有东西在重复*。

== 判据不是快慢

区分这两者的，是「前言能不能被切出来」，而不是秒表。

一个启动要十秒、但十秒全花在读你输入的程序，一分钟也不值得常驻；
一个启动只要两百毫秒、但两百毫秒全是固定开销的程序，反而值得。

== 前言可以只付一次

判断标准是「前言能不能被切出来」，那切出来之后怎么不重复付？

让一个进程先把库加载好，之后每个新任务从它 `fork`：新进程拿到的是父进程那份
已经建好的地址空间，加载和初始化一件都不用重做。

#lab("演示 09：fork 继承，还是每次 exec 重来")[
  同一个「贵」库（8 MB 数据段，构造函数里 memset 一遍），同一个任务各做 40 次：

  ```sh
  $ ./09-zygote-prefork.sh
    A 路 fork   40 次任务共花   20 ms   构造函数跑了   1 遍
    B 路 exec   40 次任务共花  192 ms   构造函数跑了  40 遍
  ```

  B 路比 A 路慢 4 到 10 倍（每次跑动的幅度不一样，看的是量级）。多出来的那 39 遍
  构造函数，就是动态链接器那四步 —— 找库、映射、重定位、调 init
  （#chapterRef("4.") 的 08 把这笔账打了出来）。
  它们只跟「这份程序映像」有关，跟这次任务要干什么毫无关系：
  fork 不 exec，子进程拿到的是父进程那份已经建好的地址空间（写时复制，
  不真的拷内存），那四步一次都不用再做。

  真实系统上 Chromium / QtWebEngine 就是这么干的：zygote 手里 227 个 .so，
  renderer 从它 fork 出来，比它多 0 个 —— 一个库都没有重新加载。

  序里体检二的那两个 zygote 模板进程，作用就是这个：它们不是被 `exec` 出来的，
  是从一个已经装好引擎的进程里 `fork` 出来的。

  #note[
    「构造函数」在这里是前言的一个具体样本：它做的事跟这次任务要干什么毫无关系。
    换成浏览器的前言，就是 Python 解释器、Qt、Chromium 引擎 —— 那 1.8 秒里的大头。
  ]

  （完整脚本：`./docs/labs/resident-browser/09-zygote-prefork.sh`）
]

那 39 遍构造函数不是被删掉了，是被挪进了一个*活得比请求久的进程*。
挪出去之后，那个进程自己的死就成了新问题。

= 3. 同一个模式，不同的管道

Ghostty 对终端做的是同一件事：一个常驻进程，加上很快的客户端，新窗口由 IPC 创建
（见 #link("https://ghostty.org/docs/linux/systemd")[Ghostty 的 systemd 集成]）。

它内部看起来跟我做的事很不一样 —— 走的是 D-Bus 和 systemd，而我是裸 Unix socket ——
但形状完全相同。因为这个形状是由判据决定的，不是由口味决定的。

#note[
  但*思路可以搬，管道不能*。上面那条历史里的四个实现，每一个的坑都长在管道上，
  而不是长在思路上。这个系列的后半段基本都在讲管道上的坑。
]

= 4. 那账单是多少

常驻是一笔内存交易，所以要付一个价：一百多 MiB（零窗口，量法见
`./docs/labs/resident-browser/12-resident-memory.sh`）。

这个数只在一处起作用：它是「值不值得」的阈值。而阈值一旦落到这个量级，
判据就不在它手里了 —— 一台能开 Chromium 的机器上，一百多 MiB 是零头。

= 5. 频率也不做决定

我原本的推理是：常驻要占内存，所以只有用得足够频繁才划算。按上面那个数算一遍，
「一个月开一次」并不明显是笔坏买卖。

#punch[
  频率几乎不做决定。真正把关的是*可分离性*。
]

把一个程序排除在外的，不是它贵，而是它*没有可拆开的前言可以保温*。
这个结论跟我一开始的直觉是反的，而且反得挺重要：它意味着
「值不值得常驻」这个问题几乎总是有同一个答案 —— 只要你有前言可切，就值得；
没有，就不值得。中间那片「要看情况」的地带比我以为的窄得多。

= 6. 没人提的代价

内存是看得见的价格。还有两个不容易被看见的，而它们造成的故障比内存难查得多。

== 常驻进程是「启动那一刻的配置」的快照

此后每一次改配置，都多出第二步：重启它。而忘记这一步，看起来跟「配置里有 bug」
*一模一样* —— 你改了，没生效，于是开始怀疑自己写错了。

你用「启动慢」换来了「静默地过期」。后者的症状是「我明明改了」，
而不是「慢」—— 前者你会去修，后者你会开始怀疑自己。

== 一个永不停止的进程，必须是你杀得掉的进程

这件事做砸了，你不会退回一个「启动慢的浏览器」，你会得到一堆攥着内存不放的
*孤儿 renderer 进程*，直到重启。

#oops[
  我当时的判断是「释放资源是个小问题，加个 namespace 就完了」。

  实际上它牵出的东西远不止释放资源 —— 而这一章教的不是 namespace，
  是*怎么判断一个工具该不该用*。
]

= 7. 判据

#punch[
  给「开销恒定且重复」的那部分做常驻。
  凡是取决于请求的东西，让它该多慢就多慢。
]

对浏览器来说，这条线落在*引擎和窗口之间*。

= 8. 这门课接下来

+   *谁来释放资源* —— 生命周期的那一半。为什么 PID namespace 是错的工具，
    以及我怎么把输入法搞丢的
+   *什么该留在 RAM* —— 这条线具体画在哪里，以及常驻到底占多少内存
+   *快路径用什么写* —— 热路径那一半，五种语言九个实现，实测

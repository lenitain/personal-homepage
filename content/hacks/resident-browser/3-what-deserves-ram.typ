#import ".course.typ": title, ask, lab, oops, note, punch, cols

#set document(title: "什么该留在 RAM")

#title[什么该留在 RAM]

上一章把「谁负责关掉它」交给了 pid 1。这一章是钱的问题。

浏览器常驻之后，一个很自然的下一步是：把它的数据也放进 RAM。这个直觉是错的，
而且错得很有意思 —— *浏览器 profile 里的大部分字节是死的*。把它们拷进内存，
你什么也没买到，只是永久性地占住了一块。

所以真正的问题不是「RAM 里能塞多少」，而是*哪些字节是活的*。

= 1. 先把收益量出来

在讨论怎么省之前，先确认这笔买卖本身是赚的。同一个 URL、同一个本地页面、
走真实 HTTP、没有网络抖动，从敲下命令到页面加载完并可见（不是「窗口出现了」），各 8 次：

#table(
  columns: (1fr, auto, auto, auto),
  table.header([场景], [中位], [最快], [最慢]),
  [冷启动（`qutebrowser`）], [1227 ms], [1167 ms], [1263 ms],
  [常驻（`qb-open`）], [232 ms], [221 ms], [254 ms],
)

消失的那一秒，是整条 Python → Qt → WebEngine 链被重建的代价。剩下的 230 毫秒
是你*真正要求它做的事*：开一个窗口，在里面加载一个页面。

现在要做的，是别把买这一秒所花的内存浪费掉。

= 2. 三种字节

#table(
  columns: (auto, 1fr, auto, 1fr),
  table.header([种类], [例子], [该在哪], [为什么]),
  [活的], [运行中的浏览器、profile 写入], [RAM], [热、小、必须持久],
  [死的缓存], [shader cache、HTTP cache], [磁盘], [可重建、大、冷],
  [页面状态], [DOM、JS、媒体、连接], [关页即释放], [属于那个页面],
)

中间那一行是钱所在的地方。那两份缓存*看起来*都像是你会想放进 RAM 的东西，
而它们都是应该被主动赶出去的。下面两节分别处理这三行。

= 3. 活的：profile，而且只留它的增量

profile 是浏览器写历史、cookie、localStorage 的地方。它小、持续、必须活过重启 ——
所以 RAM 是它正确的家。

而拿到这个结果最直觉的做法，是*错的*：登录时把整个 profile 拷到 tmpfs 上。
为什么错？因为一个 profile 里绝大多数文件在一次会话里根本不会被碰，
你会为「只被读过」的文件付内存。

== overlayfs 的做法

profile-sync-daemon 把它挂在一个 *overlayfs* 上：底下是磁盘上的只读层，
上面是 tmpfs 上的可写层。你从不写的文件仍然直接从磁盘读；第一次写某个文件时，
它才被 *copy-up* 到 tmpfs 层。

每小时一次 resync，把攒下来的增量写回磁盘上的备份。结果是
*RAM 里放的是增量，不是 profile。*

#note[
  为什么是 fuse-overlayfs 而不是内核的 overlayfs？因为挂载内核 overlayfs 需要 root，
  而 profile 在 `$HOME` 下面。fuse-overlayfs 是同一个思路的 FUSE 实现，普通用户就能挂。
  代价是每次文件操作多一层用户态往返 —— 对一个只写历史和小文件的负载，无所谓。
]

== 先停一下

profile 有 12 MB。你觉得一个常驻浏览器带着它、零窗口，会占多少内存？

随便猜一个数，再往下看 —— 下面那个数字当初把我打脸了。

= 4. 实验：量它

量这个数有个麻烦：*别打扰你正在用的那个实例*。所以起一个完全隔离的临时实例 ——
自己一个 `XDG_RUNTIME_DIR`，里面用软链把真实的那些 socket（wayland / dbus / pipewire）
*除 `qutebrowser/` 之外*全部借过来。

这样它什么都能连上（合成器、session bus、音频都正常），但它自己的 IPC socket
落在隔离目录里，既不会被 `qb-open` 扫到，也不会跟真实实例抢。再加一个独立的
`--basedir`，profile 就跟真实实例无关了。用 systemd 临时 scope 跑，还能直接读它的
cgroup 内存。

```sh
A) 空 profile、零窗口 —— 引擎本身的底价
   empty      136 MiB（三次读数: 136 136 136）  任务数 72

B) 真实 profile、零窗口
   real       140 MiB（三次读数: 140 140 140）  任务数 75

C) 正在用的那个实例（带若干标签页）
   qb-server.service   2031 MiB（峰值 4419 MiB）  任务数 117
```

连读三次是为了排除「还在爬」—— 数字不动，说明稳定了。A 和 B 两行是可复现的底价；
C 行只是你机器上那一刻的快照，开几个标签页就会变，它在这里的作用是当参照物。

#oops[
  我在这篇文章的上一版里写的是「零窗口约 700 MB 到 1 GB」。那是我凭印象写的，
  而且*错了七倍*。

  真实的底价是 140 MiB，而且换 profile 几乎不影响：空 profile 136 MiB，
  真实 profile 140 MiB。多出来的 4 MB 就是 overlayfs 在干活 ——
  一个天天在用的 12 MB profile，只有 4 MB 真的进了内存。

  那上 GB 的数字是哪来的？是我自己那个开着标签页的实例（C 行）。
  那是*用出来的*，不是常驻设计收的费。
]

#note[
  这个错误值得记一下：我引用了一个自己脑子里的数字，而不是去量它。
  它的量法其实只要二十行脚本，而且还能顺便验证 overlayfs 到底管不管用。
  *在这个系列里，凡是能量的我后来都量了* —— 这一章剩下的部分基本都是这个原则的产物。
]

= 5. 死的缓存：两个要主动赶走的东西

== GPU shader cache

QtWebEngine 会在 profile 边上写一份编译好的 shader 缓存，而且*每次启动都重写一遍*。

在 PSD 的 overlay 之下，「每次启动都重写」等于「每次启动都 copy-up 进 RAM」——
一块又大又脏、且无法回收的内存，换来的是省下一些本来就不会重复的工作。
所以关掉它（`c.qt.args = ["disable-gpu-shader-disk-cache"]`），shader 改成进程内编译。

#note[
  ⚠️ 但注意这笔交易的前提：*它只有在浏览器常驻之后才是免费的。*

  如果每次都是冷启动，你会在每次启动时重新编译 shader，付的是实打实的时间。
  而一旦只有一个长期活着的实例，shader 一次会话只编译一次，磁盘缓存就没有东西可省了。

  也就是说 —— *daemon 化这个决定，才是这条优化的前提*。
  这是这个系列里第一次出现「一个决定让另一个优化变得合法」的情况，后面还有。
]

== HTTP 磁盘缓存

这一份确实值得大，也确实应该在磁盘上。它存的是页面资源，能到几百兆 ——
光是视频分片就够了；全部可以重新抓取，淘汰策略是 LRU。所以它是*名副其实的缓存*。

而它该在磁盘上的理由是：在磁盘上它花的是 page cache，内核在内存紧张时可以丢掉；
放在匿名的 tmpfs 里，它花的是*谁也回收不了的内存*。

所以：给它设个上限，放在磁盘上，让内核决定这些页值不值得留着。

= 6. 页面状态：关页即走

DOM、JavaScript 堆、解码后的媒体、打开的连接，都跟着标签页一起死。没有一样比创建它的
页面活得更久，也没有一样需要你去管。

== 一件看起来像泄漏、其实不是的事

关掉所有窗口之后，还有一个 QtWebEngine 的 renderer 进程留在那儿。

它*没有*攥着任何已关闭的页面 —— 它是等着下一个页面的共享基础设施，
而这正是常驻设计的目的。如果你用「关窗之后进程数应该归零」当验收标准，
你会一直以为这里有 bug。

= 7. 结论

#punch[
  快不是「把所有东西都塞进 RAM」，
  而是分清哪些是活的、哪些是死的，只给前一种付内存。
]

= 8. 下一章：快路径用什么写

到这里，常驻的部分（服务端）和它的代价（内存）都说完了。

剩下的是那条*热路径*：你按下快捷键之后、到消息送达之间，那几百微秒是怎么花掉的。

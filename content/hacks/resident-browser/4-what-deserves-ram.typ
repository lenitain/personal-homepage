#import ".course.typ": title, lab, note, oops, punch

#set document(title: "什么该留在 RAM")

#title[什么该留在 RAM]

上一章把「谁负责关掉它」交给了 pid 1。这一章是开销的问题。

浏览器常驻之后，一个很自然的下一步是：把它的数据也放进 RAM。
但浏览器手里那几样数据，该待的地方各不相同：

+   *这一次会话写下的历史、cookie、localStorage* —— 丢了就没了，而且马上还要用。
    内存里要有一份，磁盘上也必须有一份。
+   *编译好的 shader、下载过的网页资源* —— 随时能重做，只是重做一遍不便宜。
    留在磁盘上就够了：内核会把磁盘上的内容当缓存用，热的留着，冷的丢掉。
+   *DOM、JS 堆、解码后的媒体* —— 跟着标签页一起消失，不用你管。

所以真正的问题不是「RAM 里能塞多少」，而是这几样东西各该在哪儿。

= 1. 先确认这笔买卖本身是赚的

常驻省掉的是*前言*：引擎、Qt、解释器、模块，整条链重新建一遍的那一段。
体检量到它长什么样 —— 这是 `qutebrowser` 这个命令在付完前言之后、动手做你要的事之前的全部家当：

#lab("演示 13：一个「什么都不干」的请求，前面站着多少东西")[
  ```sh
  它是什么     Python 脚本，970 字节，头一行 #!/usr/bin/python3
  import 账单  109 个模块，最外层累计 62 ms；自身最贵的是 _colorize（5.2 ms，跟浏览器无关）
  ```

  上面这个请求是 `about:blank` —— 没有页面、没有网络、没有脚本。
  而这 109 个模块、整条 Qt 链、整个 Chromium 引擎，一个都不能少。

  #note[模块导入这一段在 `strace` 下跑，绝对值比平时慢。要看的是账单位置，不是毫秒数。]

  （完整脚本：`./docs/labs/resident-browser/13-qutebrowser-checkup.sh`）
]

常驻之后，这一整套不再重建。省下来的正是它 ——
而*剩下的那一小截*，才是你真正要求它做的事：开一个窗口，在里面加载一个页面。

所以接下来的问题不是「还能不能更快」，而是：别把买这一秒所花的内存浪费掉。

= 2. 三样东西，各该在哪儿

#table(
  columns: (1fr, auto, 1fr),
  table.header([东西], [该在哪], [为什么]),
  [这一次会话写下的历史、cookie], [RAM 和磁盘], [丢了就没了、马上还要用],
  [shader cache、HTTP cache], [磁盘], [随时能重做、又大又冷],
  [DOM、JS 堆、媒体], [关页即释放], [跟着标签页一起消失],
)

前两行是要你做的决定，第三行不是 —— 它跟着标签页一起消失。

而开销集中在第二行：那两份缓存*看起来*都像是你会想放进 RAM 的东西，
而它们都是应该被主动赶出去的。

= 3. profile：只留这一次写下的那部分

profile 是浏览器写历史、cookie、localStorage 的地方 —— 第一样东西住在那里。
它小、持续、必须活过重启，所以内存里该有它一份。

而拿到这个结果最直觉的做法，是*错的*：登录时把整个 profile 拷到 tmpfs 上。

错在「整个」。一个 profile 里绝大多数文件是以前留下的，这一次会话根本不会碰；
而把它们拷进内存，换到的也只是省掉两次读 —— 第一次读，和缓存被挤掉之后的再读。
第一次读无论如何都要发生，而再读之所以少见，正是因为*内核本来就会把读过的文件页
留在内存里当缓存*：放在磁盘上，并不等于每次都读盘。既然内核已经在做这件事，
把它按进 tmpfs 就没有多换来什么。

代价却是永久的。tmpfs 里的页没有后备存储，内核不能丢掉它们腾地方，
也不能拿它们去缓存别的页 —— 内存紧张时，只能去动别的东西。
等于把一块内存从「可回收」变成了「钉死的」。

要放进 RAM 的，是这一次写下去的那部分。

== overlayfs 的做法

profile-sync-daemon 把它挂在一个 *overlayfs* 上：底下是磁盘上的只读层，
上面是 tmpfs 上的可写层。你从不写的文件仍然直接从磁盘读；第一次写某个文件时，
它才被 *copy-up* 到 tmpfs 层。

每小时一次 resync，把攒下来的增量写回磁盘上的备份。结果是
*RAM 里放的是增量，不是 profile。*

#note[
  为什么是 fuse-overlayfs 而不是内核的 overlayfs？因为挂载内核 overlayfs 需要 root，
  而 profile 在 `$HOME` 下面。fuse-overlayfs 是同一个思路的 FUSE 实现，普通用户就能挂。
  代价是每次文件操作多一层用户态往返 —— 对一个只写历史和小文件的负载，无所谓。
]

= 4. 量它，而且别打扰你正在用的那个实例

量这个数有个麻烦：你不想在测量的时候动到自己正在用的浏览器。所以起一个完全隔离的临时实例 ——
自己一个 `XDG_RUNTIME_DIR`，里面用软链把真实的那些 socket（wayland / dbus / pipewire）
*除 `qutebrowser/` 之外*全部借过来。

这样它什么都能连上（合成器、session bus、音频都正常），但它自己的 IPC socket
落在隔离目录里，既不会被 `qb-open` 扫到，也不会跟真实实例抢。再加一个独立的
`--basedir`，profile 就跟真实实例无关了。用 systemd 临时 scope 跑，还能直接读它的 cgroup 内存。

#lab("演示 12：零窗口、只换 profile，底价会动吗")[
  ```sh
    空 profile       129 MiB   三次读数 129 129 129   任务数 69
    真实 profile     131 MiB   三次读数 131 131 131   任务数 73
    （B 读的是真实 profile 的只读拷贝，556K，原件没碰）
  ```

  连读三次是为了排除「还在爬」—— 数字不动，说明稳定了。
  换成真实 profile，底价只动了 2 MiB，而整个 profile 只有 556 KB。

  （完整脚本：`./docs/labs/resident-browser/12-resident-memory.sh`）
]

一个天天在用的 profile 只有 556 KB —— 小到不需要任何技巧。
*开销不在这里。* 真正会让 RAM 变紧的，是下面那两份看起来也属于「浏览器数据」的东西。

#oops[
  我在这一章的上一版里写的是「零窗口约 700 MB 到 1 GB」。那是我凭印象写的，
  量出来是一百多兆。

  而且换 profile 几乎不影响。
  那上 GB 的数字，来自我自己那个开着标签页的实例 —— 那是*用出来的*，
  不是常驻设计收的费。
]

#note[
  这个错误的成因是：我引用了一个自己脑子里的数字，而不是去量它。
  它的量法其实只要二十行脚本，而且还能顺便验证 overlayfs 到底管不管用。
]

= 5. 两份缓存：要主动赶走的两个东西

== GPU shader cache

QtWebEngine 会在 profile 边上写一份编译好的 shader 缓存，而且*每次启动都重写一遍*。

在 overlay 之下，「每次启动都重写」等于「每次启动都 copy-up 进 RAM」——
一块又大又脏、且无法回收的内存，换来的是省下一些本来就不会重复的工作。
所以关掉它（`c.qt.args = ["disable-gpu-shader-disk-cache"]`），shader 改成进程内编译。

#note[
  ⚠️ 但注意这笔交易的前提：*它只有在浏览器常驻之后才是免费的。*

  如果每次都是冷启动，你会在每次启动时重新编译 shader，付的是实打实的时间。
  而一旦只有一个长期活着的实例，shader 一次会话只编译一次，磁盘缓存就没有东西可省了。

  也就是说 —— *daemon 化这个决定，才是这条优化的前提*。
]

== HTTP 磁盘缓存

这一份确实值得大，也确实应该在磁盘上。它存的是页面资源，视频分片就能把它撑得很大；
全部可以重新抓取，淘汰策略是 LRU。所以它是*名副其实的缓存*。

而它该在磁盘上的理由是：在磁盘上它花的是 page cache，内核在内存紧张时可以丢掉；
放在匿名的 tmpfs 里，它花的是*谁也回收不了的内存*。

所以：给它设个上限，放在磁盘上，让内核决定这些页值不值得留着。

= 6. 页面状态：关页即走

DOM、JavaScript 堆、解码后的媒体、打开的连接，都跟着标签页一起死。没有一样比创建它的
页面活得更久，也没有一样需要你去管。

== 关窗后留下的那个渲染进程不是泄漏

关掉所有窗口之后，还有一个 QtWebEngine 的渲染进程留在那儿。

它*没有*攥着任何已关闭的页面 —— 它是等着下一个页面的共享基础设施，
而这正是常驻设计的目的。如果你用「关窗之后进程数应该归零」当验收标准，
你会一直以为这里有 bug。

= 7. 结论

#punch[
  快不是「把所有东西都塞进 RAM」，
  而是让每一样数据待在它该待的地方。
]

= 8. 下一章：快路径用什么写

到这里，常驻的部分（服务端）和它的代价（内存）都说完了。

剩下的是那条*热路径*：你按下快捷键之后、到消息送达之间，那几百微秒是怎么花掉的。

#import "../.course.typ": note, punch, slide, title

#set document(title: "什么样的程序值得常驻（讲义）")

// 切法跟文档版不同：一张幻灯片只承载一个「讲到这里要让人记住的点」。
// 跑出来的结果不进讲义 —— 讲课的时候当场跑，讲义上只留脚本路径。
// 标题是关于主题的断言，不是关于我自己的叙事。

#slide[
  #title[什么样的程序值得常驻]

  上一章把那 1.2 秒切成了两段，两段都跟你要打开的页面无关。

  *那么，这两段能不能只付一次？*
]

#slide[
  = 常驻是一笔内存交易，不是提速技巧

  每一篇讲「让慢程序变快」的文章都会给同一个建议：
  启动一次，让它一直跑着。

  但*启动开销并没有被删掉* —— 你是一次付清，
  然后为那个结果永久付租金。
]

#slide[
  = 这个思路有四十年历史

  - inetd —— 按需拉起网络服务，然后把连接交给它
  - Emacs server —— 编辑器太贵，常驻一个
  - systemd socket activation —— init 先拿着 socket
  - Ghostty / qutebrowser —— 同一个形状，换了个管道

]

#slide[
  = 判据是：慢的那部分能不能跟请求拆开

  #punch[
    存不存在一大块工作，它在程序知道你想要什么*之前*就做完了，
    而且*每一次都产出完全相同的结果*？
  ]
]

#slide[
  = 浏览器有可切的前言，编译器没有

  *浏览器* —— 解释器、Qt、Chromium 引擎、adblock 规则、profile。
  你接下来要开一个标签页还是五十个，这些完全一样。

  *编译器* —— 时间花在读*你的*源码、产出*你的*二进制上。
  而这些工作在你开口说「编译什么」之前根本没法开始。

  #note[现场跑：`./docs/labs/resident-browser/13-qutebrowser-checkup.sh`]
]

#slide[
  = 判据不是快慢

  区分这两者的是「前言能不能被切出来」，而不是秒表。

  #punch[
    启动十秒、但十秒全花在读你输入的程序 → 不值得常驻。
    启动两百毫秒、但两百毫秒全是固定开销 → 值得。
  ]
]

#slide[
  = 零窗口的底价是一百多兆

  常见说法是「零窗口 700 MB 到 1 GB」。*错了七倍。*

  而换成真实 profile 几乎没动 —— 你付的也不是 profile 的开销。

  #note[现场跑：`./docs/labs/resident-browser/12-resident-memory.sh`]
]

#slide[
  = 上 GB 的那些字节是用出来的

  页面 renderer 和它们的 JavaScript 堆会随着浏览不断累积。

  那不是常驻的锅 —— 一个*不常驻*的浏览器开着同样这些标签页，一样要付。
  常驻设计只多收了那个底价。
]

#slide[
  = 频率几乎不做决定

  - 一天开 20 次：换回约 20 秒/天，代价一百多兆
  - 一个月开一次：为省 1 秒，一直占着一百多兆

  在一台*跑得动这个浏览器*的机器上，第二行不明显是笔坏买卖。

  #punch[真正把关的是可分离性。]
]

#slide[
  = 常驻进程是「启动那一刻的配置」的快照

  此后每一次改配置，都多出第二步：重启它。

  忘记这一步的症状是「我明明改了」，而不是「慢」——
  前者你会去修，后者你会开始怀疑自己。
]

#slide[
  = 一个永不停止的进程，必须是你杀得掉的进程

  这件事做砸了，你不会退回一个「启动慢的浏览器」，
  你会得到一堆攥着内存不放的*孤儿 renderer*，直到重启。
]

#slide[
  #punch[
    给「开销恒定且重复」的那部分做常驻。
    凡是取决于请求的东西，让它该多慢就多慢。
  ]

  对浏览器来说，这条线落在*引擎和窗口之间*。
]

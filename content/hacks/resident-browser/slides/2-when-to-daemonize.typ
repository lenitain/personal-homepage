#import "../.course.typ": title, slide, punch, cols, note

#set document(title: "什么样的程序值得常驻（讲义）")

#slide[
  #title[什么样的程序值得常驻]

  #note[对应文档版第一章。左边文件树里 `1-when-to-daemonize.typ` 是完整版。]
]

#slide[
  = 一个被当成提速技巧的内存交易

  每篇讲「让慢程序变快」的文章都会说：

  #quote(block: true)[
  启动一次，让它一直跑着。
  ]

  但*你并没有把启动开销删掉* ——
  你是把它一次付清，然后为那个结果*永久付租金*。
]

#slide[
  = 这不是新发明

  - `inetd` —— 按需拉起网络服务
  - Emacs server —— 编辑器太贵，常驻一个
  - systemd socket activation —— init 先拿着 socket
  - Ghostty / qutebrowser —— 同一个形状

  #punch[一条四十年的思路反复出现，说明背后有个稳定判据，而不是潮流。]
]

#slide[
  = 那个判据

  #punch[慢的那部分，跟你要求它做的事，能不能拆开？]

  具体说：存不存在一大块工作，
  它在程序知道你想要什么*之前*就做完了，
  而且*每次都产出相同的结果*？
]

#slide[
  = 两个例子

  #cols[
    *浏览器 —— 存在*
    ```sh
    解释器 / Qt / 引擎
    adblock 规则 / profile
    → 约一秒，每次一样
    ```
  ][
    *编译器 —— 不存在*
    ```sh
    启动是舍入误差
    时间花在读你的源码
    → 你开口之前没法开始
    ```
  ]
]

#slide[
  = 判据

  区分这两者的是「前言能不能被切出来」，
  而不是秒表。

  #punch[
    启动十秒但全花在读你输入的程序 → 不值得常驻。
    启动两百毫秒但全是固定开销 → 值得。
  ]
]

#slide[
  = 那账单是多少

  我记成「零窗口 700 MB 到 1 GB」。

  ```sh
  零窗口，真实 profile     140 MiB
  开着若干标签页           约 2 GiB
  ```

  *错了七倍。*
]

#slide[
  = 而且你付的不是 profile 的钱

  ```sh
  空 profile      136 MiB
  真实 profile    140 MiB
  ```

  差 4 MB。上 GB 的是*用出来的* ——
  一个不常驻的浏览器开着同样这些标签页，一样要付。
]

#slide[
  = 于是频率这个判据很弱

  - 一天开 20 次：约 20 秒/天，代价 140 MiB
  - 一个月开一次：为省 1 秒，占着 140 MiB

  在一台跑得动这个浏览器的机器上，第二行不明显是坏买卖。

  #punch[频率几乎不做决定。真正把关的是可分离性。]
]

#slide[
  = 没人提的两个代价

  *一、常驻进程是「启动那一刻的配置」的快照*

  此后每次改配置都多出第二步：重启它。
  忘记这一步，看起来跟「配置里有 bug」一模一样。

  *二、一个永不停止的进程，必须是你杀得掉的进程*
]

#slide[
  = 第二条我踩进去了

  做砸了不会退回一个「启动慢的浏览器」，
  而是得到一堆攥着内存不放的*孤儿 renderer*。

  #note[这就是下一章的全部内容 —— 而且它连带出的东西比预想的多得多。]
]

#slide[
  #punch[
    给「开销恒定且重复」的那部分做常驻。
    凡是取决于请求的东西，让它该多慢就多慢。
  ]

  对浏览器，这条线落在*引擎和窗口之间*。
]

#import "../.course.typ": title, slide, punch, cols, note

#set document(title: "快路径用什么写（讲义）")

#slide[
  #title[快路径用什么写]

  同一个启动器，五种语言九个实现

  #note[对应文档版第四章。完整版见 `4-what-to-write-it-in.typ`。]
]

#slide[
  = 最无聊的程序，最值得测

  启动器干的全部事情：

  ```sh
  找一个 socket → 写一百个字节 → 退出
  ```

  而这正是它值得测的原因 ——
  把程序剥掉之后，剩下的是
  *在某门语言里「成为一个进程」的代价*。
]

#slide[
  = 方法

  - *正确性把关*：九种实现必须跟 C 版*逐字节相同*，八个用例全过
  - *延迟*：`fork`→`execve`→干活→回收，绑核、三轮 × 500 次、报最小值
  - *底噪*：一个只 `exit_group` 的静态二进制 —— 没有它，「300 µs」没法解释
  - *地址空间*：在 `execve` 成功那一刻用 ptrace 停住再读
]

#slide[
  = 数字（微秒）

  #table(
    columns: (1fr, auto, auto, auto, auto, auto),
    table.header([实现], [最快], [超底噪], [syscall], [VMA], [体积]),
    [底噪], [224], [—], [2], [8], [8.5 KB],
    [汇编], [*288*], [*+63*], [*10*], [*9*], [8.9 KB],
    [C musl 静态], [317], [+93], [20], [10], [*46 KB*],
    [Zig 静态], [442], [+218], [15], [9], [7.9 KB],
    [Rust 静态], [469], [+245], [47], [10], [1.35 MB],
    [Python], [21402], [+21178], [862], [14], [2.3 KB],
  )
]

#slide[
  = 真正干的活只有 63 微秒

  汇编减底噪。找一个 socket、写一条消息、退出 ——
  在这个尺度上相对「存在的代价」是个舍入误差。

  #punch[
    为一个热路径启动器选语言，不是在选语法，
    而是在选「什么东西会跟着你一起启动」。
  ]
]

#slide[
  = 静态链接三比零

  #cols[
    *动态*
    ```sh
    C musl    393
    C glibc   586
    Rust      751
    ```
  ][
    *静态*
    ```sh
    C musl    317
    C glibc   468
    Rust      469
    ```
  ]

  动态版本要为 `ld.so` 的启动、符号解析、映射库付钱。
]

#slide[
  = 但静态是预付的

  - glibc 静态：*826 KB 对 15 KB* —— 56 倍字节换 118 µs
  - musl 静态：*46 KB* —— 这才是划算的那个版本

  体积不只是磁盘：exec 时刻的 RSS 几乎没动，
  但映射页数动了 —— glibc 静态版干活之前就映射了 1 MB。
]

#slide[
  = 两个极端

  *Zig* —— 9 个 VMA、12 KB RSS，跟手写汇编*完全一样*。
  没有运行时需要初始化。

  *Rust* —— 即使 strip 过，静态版还是 1.35 MB、47 个 syscall
  （六个 `rt_sigaction`、五个 `brk`）。那是 `std` 在 `main` 之前搭运行时。
]

#slide[
  = Python 是汇编版的 74 倍

  ```sh
  python3 -c pass           10.6 ms
  + import socket           14.5 ms   (+3.9)
  + import json             18.8 ms   (+8.1)
  完整的启动器              22.5 ms   (+0.6)
  ```

  #punch[
    大约 3% 的运行时间是程序被写出来要做的那件事。
    这就是当初值得写一个 C 客户端的全部理由 —— 从来不是因为 C 快。
  ]
]

#slide[
  = 每个运行时都写在 syscall 记录里

  - *汇编* —— 10 个，正好就是那件工作，没有别的
  - *C musl 静态* —— 20 个，多的 10 个是 libc 初始化
  - *Zig* —— 15 个，含 `sigaltstack`、`prlimit64`
  - *Rust 静态* —— 47 个
  - *glibc 动态* —— 42 个，被 `ld.so` 占满
  - *Python* —— 862 个，形状就是导入机制
]

#slide[
  = 我选了什么

  *C，musl，静态* —— 46 KB，317 µs，20 个 syscall，一条 `musl-gcc -static`。

  汇编小 37 KB、快 29 µs，代价是 400 行手写汇编。
  Zig 能用写得动的语言拿到汇编的进程像 ——
  代价是把构建钉死在某个 Zig 版本上。
]

#slide[
  = 写五个实现，抓出一个真 bug

  C 版用 `snprintf` 拼 JSON，然后把返回值加进写入偏移量。
  而它返回的是*本应写入*的长度 ——

  截断 → 偏移量冲过缓冲区末尾 → `sizeof(buf) - pos` 下溢成 `SIZE_MAX`
  → 下一次写入落在 `.bss` 里紧挨着 `environ` 的地方。

  #note[单个参数 ≥ 约 8.1 KB 就会触发，而这段代码已经发布、每天都在用。]
]

#slide[
  = 后果不是理论上的

  - 多数情况：`write()` 读到未映射页失败 → fallback *冷启动一整个真浏览器*
  - 8124–8130 字节：`environ` 被写成 `,"cwd":` → `execve` 直接 `EFAULT`，
    什么都没干就退出 1

  #punch[每个其它语言的移植版从一开始就做了边界检查 —— 这是它唯一浮出水面的原因。]
]

#slide[
  #punch[
    对一个热路径上的程序，你不是在选一门语言。
    你是在选「在你的代码跑起来之前，有什么必须启动」——
    而答案是：全部。
  ]
]

#import "../../../.course.typ": ask, note, oops, punch, slide, title

#set document(title: "快路径用什么写（讲义）")

// 切法跟文档版不同：一张幻灯片只承载一个「讲到这里要让人记住的点」。
// 跑出来的结果不进讲义 —— 讲课的时候当场跑，讲义上只留脚本路径。
// **一个例外：那张对比表。** 它是这一章要讲的东西本身，散在十几张幻灯片里
// 讲一遍，听众带不走；所以它整张进讲义，数字跟正文那两张表是同一批（有测试钉着）。
//
// 这一份的线：要比的是什么 → 怎么比才成立 → 机制 → 九种实现 → 选谁 → 附带那个 bug。
// 跟文档版第七节一样，那个 bug 排在结论之后 —— 它是这件事的副产品，不是决定本身。

#slide[
  #title[热路径用什么写]

  常驻的部分（服务端）和它的代价（内存）都说完了。

  剩下那条*热路径*：从敲下命令、到消息送达之间，那几百微秒。
  这一段归启动器管，于是要做一个决定：*用它什么写*。
]

#slide[
  = 启动器只做三件事

  找到特定的 socket 文件，写大约一百个字节，退出。

  程序里的工作量小到可以忽略 —— 于是剥掉它之后，
  剩下的是*在某门语言里「成为一个进程」的代价*。

  所以比的不是「哪门语言好」，
  而是每门语言在你的代码跑起来之前做了什么。
]

#slide[
  = 先过一道正确性

  九个实现都必须跟 C 版吐出*逐字节相同*的 IPC 消息，八个用例全过：
  两个参数、无参数、空参数、引号与反斜杠、换行与制表符、
  C0 控制字符、裸 UTF-8、2000 字节的长参数。

  #punch[一个因为发错字节而变快的启动器，不是启动器。]

  #note[现场跑：`./docs/labs/resident-browser/verify.sh`]
]

#slide[
  = 先减掉「底噪」，再看程序干了多少

  底噪是一个静态二进制，函数体只有 `exit_group`：它什么都不干，
  所以它测出来的就是*每一个实现都要付的「被启动」的开销*。

  汇编版 309.9 µs 减掉底噪 138.8 µs，剩下的 171 µs 才是全部工作：
  找 socket、写一条消息、退出 —— 相对「存在的代价」是个舍入误差。

  #note[
    现场跑：`./docs/labs/resident-browser/latency.sh out 3 500`。
    口径是三轮 × 500 次、绑核、报最小值 —— 不绑核，读数能差一半。
  ]
]

#slide[
  #punch[
    为一个热路径启动器选语言，不是在选语法或者表达力，
    而是在选「什么东西会跟着你一起启动」。
  ]
]

#slide[
  = 跟着一起启动的，是动态链接器

  用了别人写好的库的程序，文件里写着「我需要一个解释器」——
  内核建好地址空间之后，*跳转的是那个解释器*，原程序只是作为参数被交出去。

  它每启动一个程序都要做四件事：找库、把库映射进地址空间、修正地址、调各家的初始化函数。
  四件事都跟你程序做什么无关。

  #note[现场跑：`./docs/labs/resident-browser/08-dynamic-loader.sh`]
]

#slide[
  = 同一份源码，静态和动态差一百多微秒

  两个都只打印一行字的程序各跑 400 次：静态 372 µs，动态 495 µs，差 123 µs。

  差出来的那一截全是链接器的活。而且它跟程序要做什么无关 ——
  程序只调了一个函数，该找的库一个都不能少找。

  #note[
    现场跑：`./docs/labs/resident-browser/07-elf-loading.sh`。
    这两个数每次跑会动几十微秒，要看的是差值那个量级。
  ]
]

#slide[
  = 静态链接在每一组对比里都赢

  同样一份 `qb-open.c`：动态版 42 次系统调用，静态版 25 次 ——
  多出来的 17 次里，8 次是 `mmap`（映射内存），其余是在库缓存里翻找。

  #note[现场跑：`./docs/labs/resident-browser/measure.sh out 1000`]
]

#slide[
  = 静态是预付的：826 KB 换 160 微秒

  C 的标准库有两个实现：glibc（默认那个，大）和 musl（小）。
  同样是 glibc：静态版 826 KB，动态版 15 KB —— 56 倍的字节换 160 微秒。

  体积不只是磁盘：`execve` 之后那一刻，glibc 静态版已经映射了 1 MB。

  #note[现场跑：`./docs/labs/resident-browser/build-all.sh`]
]

#slide[
  = 两张表，两种口径，不能混着比

  同一个 `qb-open-c-musl-static`：
  绑核取最小值是 *377.6 µs*，不绑核取 p50 是 *543.4 µs*。

  延迟结论只看绑核那张；系统调用和地址空间那张不绑核，看的是另一回事。

  #note[现场跑：`./docs/labs/resident-browser/measure.sh out 1000`]
]

#slide[
  = 九种实现，一张表

  #table(
    columns: (auto, auto, auto, auto, auto, auto),
    table.header([实现], [链接], [最小延迟 µs], [系统调用], [VMA], [体积]),
    [floor（只 `exit_group`）], [—], [138.8], [—], [—], [—],
    [`qb-open-asm`], [静态], [309.9], [10], [9], [8.9 KB],
    [`qb-open-c-musl-static`], [静态], [377.6], [20], [10], [46 KB],
    [`qb-open-c-glibc-static`], [静态], [427.7], [25], [10], [826 KB],
    [`qb-open-c-musl-dyn`], [动态], [441.5], [20], [15], [14 KB],
    [`qb-open-zig`], [静态], [459.5], [15], [9], [7.9 KB],
    [`qb-open-rust-static`], [静态], [511.9], [48], [10], [1.35 MB],
    [`qb-open-c-glibc-dyn`], [动态], [588.1], [42], [15], [15 KB],
    [`qb-open-rust-dyn`], [动态], [744.2], [76], [14], [394 KB],
    [`qb-open-python`], [—], [22741.0], [867], [14], [2.3 KB],
  )

  #note[
    VMA 是地址空间里的一段区间 —— 连续、权限相同，`/proc/<pid>/maps` 里就是一行。
    延迟是 `latency.sh` 绑核、三轮 × 500 次的*最小值*（第一行是那个只 `exit_group`
    的底噪）；系统调用和 VMA 来自 `measure.sh` 在 `execve` 停点上的读数，那支不绑核；
    体积是 strip 之后的字节数（`build-all.sh`）。
    *两列口径不同 —— 只在同一列里比大小。*
  ]
]

#slide[
  = Zig 的进程跟手写汇编一样轻

  *地址空间里 9 个区间、启动那一刻常驻内存 12 KB*，
  跟手写汇编完全一样，系统调用 15 对 10。

  没有运行时需要初始化 —— 这一点不需要看代码，看 `/proc` 就知道。

  #note[现场跑：`./docs/labs/resident-browser/measure.sh out 1000`]
]

#slide[
  = Rust 的启动开销来自标准库

  即使 strip 过，静态版还是 1.35 MB 和 48 个系统调用，
  其中六个 `rt_sigaction`（装信号处理）、五个 `brk`（要内存）。

  那是 Rust 的标准库（`std`）在你的 `main` 之前搭运行时 ——
  而这里 `main` 做的事，是往一个 socket 写一百个字节。

  #note[现场跑：`./docs/labs/resident-browser/measure.sh out 1000`]
]

#slide[
  = 系统调用的形状，说出每门语言在忙什么

  汇编那 10 个，正好就是那件工作：`openat`、`socket`、`write`、`exit`，没有别的。
  Zig 15 个，多一对 `sigaltstack` / `prlimit64`。
  glibc 动态那 42 个，被 `ld.so` 找库、映射库占满。
  Rust 静态那 48 个里，六个 `rt_sigaction`、五个 `brk`。

  然后 Python：*867 个*，形状就是导入机制。
]

#slide[
  = Python 是汇编版的 73 倍

  #punch[
    大约 3% 的运行时间是程序被写出来要做的那件事。
    这就是当初值得写一个 C 客户端的全部理由 —— 从来不是因为 C 快。
  ]

  #note[现场跑：`./docs/labs/resident-browser/python-split.sh`]
]

#slide[
  = 解释器本身 10.9 毫秒，导入两个模块 11.0 毫秒

  导入比解释器还贵，而 `json` 一个就顶两个 `socket`。

  程序真正干的活 —— 扫一个目录、拼一个小对象、写进 socket —— 只花 0.7 毫秒。
]

#slide[
  = 选 C，musl，静态

  *46 KB，377.6 微秒，20 个系统调用*，
  整个构建是一条 `musl-gcc -static` 命令。

  汇编小 37 KB、快 68 微秒，代价是 400 行手写汇编。
]

#slide[
  #ask[
    如果你只需要「快」，汇编比 C 版快 68 微秒、小 37 KB。

    这 68 微秒值 400 行手写汇编吗？
  ]
]

#slide[
  = 附带：多写几份实现，抓出一个真 bug

  C 版用 `snprintf` 拼 JSON，再把返回值加进写入偏移量。
  而 `snprintf` 返回的是它*本应写入*的长度 —— 一旦截断，
  偏移量跳过缓冲区末尾，`sizeof(buf) - pos` 下溢成 `SIZE_MAX`。

  越界之后它不崩溃、不报错，而是*行为变了*：
  参数到 8043 字节还能发出去，从 8044 起它走 fallback，冷启动一整个真浏览器，
  而退出码从头到尾都是 0。用户看到的现象是「怎么突然变慢了」。

  #note[现场跑：`./docs/labs/resident-browser/overflow-check.sh`（它自己会进 mount namespace）]
]

#slide[
  #oops(label: "计划外的收获")[
    这一段不是设计出来的：写第二份实现的时候，
    撞出一个已经发布、每天都在用的内存越界写。

    它能露出来，只是因为同一个行为被写了两遍 ——
    每个移植版本都从头做了边界检查，C 版那处没做的地方才有人质疑。

    对比表是这一章要讲的东西；这个 bug 是它顺手给我的东西。
  ]
]

#slide[
  #punch[
    对一个热路径上的程序，你不是在选一门语言。
    你是在选「在你的代码跑起来之前，有什么必须启动」——
    而那份清单上写着什么，取决于你选了哪一门。
  ]
]

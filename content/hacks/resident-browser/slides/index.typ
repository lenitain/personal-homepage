#import "../.course.typ": title, slide, punch, note
#import "../.syllabus.typ": preface, chapters

#set document(title: "一个已经开着的浏览器（讲义）")

#slide[
  #title[一个已经开着的浏览器]

  qutebrowser 把启动从 1227 ms 压到 232 ms 的全过程

  #note[
    这是幻灯片版。`slides/` 下每份对应正文的一节：序一份，四章各一份。
    上一级目录里的是文档版 —— 同一批内容的完整论证，用来自学。
  ]
]

#slide[
  = 这门课反复在做同一件事

  #punch[
    拿到一个「顺手」的机制，
    然后问它原本是为哪个问题造的。
  ]
]

#slide[
  = 为什么这件事值得单独教

  一个错的工具如果当场就报错，你会立刻换一个。

  #punch[它要是能用，你就会一直在它上面打补丁。]

  #note[
    这门课里最贵的坑就是一个「能用」的错工具：我拿 PID namespace 做进程回收，
    它确实能回收 —— 代价是我的输入法没了，而且我花了一整章才想明白为什么
    （《谁来释放资源》）。
  ]
]

#slide[
  = 四章，外加一个序

  序把所有工具交到手上，不下判断：fork/execve、虚拟地址空间，
  加一次体检（那一秒花在哪）。

  四章是四个各有结论的问题：

  #for c in chapters() [
    + #c.title —— #c.notes.at(0)
  ]
]

#slide[
  = 三起事故，一个教训

  - 我凭印象把常驻开销记成「700 MB 到 1 GB」，量出来是 129 MiB
  - 我以为多语言实现的价值在对比表，实际价值在它抓出的*内存越界写*
  - 一个实验「跑通了」但完全无效（`$$` 被 systemd 展开成了字面 `$`）

  #punch[三次都是拿脑子里的模型代替了观测。]
]

#slide[
  = 怎么讲

  每一章都是同一个节奏：

  提问 → 我的猜测（含猜错的）→ 可跑的实验 → 原始输出 →
  哪里跟预期不一样 → 一条能带走的判断（序只有前三段 —— 它不下判断）

  文档版里那些「先停一下」，是留给读者自己先回答的。
]

#slide[
  #punch[
    把开销恒定的那部分保温，
    把它的生命周期交给一个本来就管生命周期的东西，
    把能重做的东西赶回磁盘。
  ]
]

#import ".course.typ": title, think, wrong, lab

#set document(title: "什么样的程序值得常驻")

#title[什么样的程序值得常驻]

每一篇讲「怎么让慢程序变快」的文章，最后都会给同一个建议：启动一次，让它一直跑着，
用 socket 跟它说话。

这个办法管用。但它通常是个错误的默认选择 —— 因为它被当成*提速技巧*推销，
而它实际上是一笔*内存交易*。

你并没有把启动开销删掉。你是把它一次付清，然后为那个结果*永久付租金*。
这笔买卖划不划算，取决于一件比「这个程序慢不慢」具体得多的事。

= 真正决定这件事的问题

不是*它有多慢*，而是：

#quote(block: true)[
  慢的那部分，跟你要求它做的事，*能不能拆开*？
]

说得具体一点：存不存在一大块工作，

- 在程序知道你想要什么*之前*就做完了，而且
- 每一次都产出完全相同的结果？

对浏览器来说，存在。Python 解释器、Qt、Chromium 引擎、adblock 规则、profile ——
不管你接下来要开一个标签页还是五十个，这些完全一样。它们是纯粹的前言（preamble）。
在我这台机器上，大约一秒。

对编译器来说，不存在。启动本身是个舍入误差。时间花在读*你的*源码、产出*你的*二进制上 ——
而这些工作在你开口说「编译什么」之前根本没法开始。没有东西可以保温，因为没有东西在重复。

*这个区别，而不是秒表，才是把两者分开的东西。*

= 同一个模式，不同的管道

Ghostty 对终端做的是同一件事：一个常驻进程，加上很快的客户端，新窗口由 IPC 创建
（见 #link("https://ghostty.org/docs/linux/systemd")[Ghostty 的 systemd 集成]）。

它内部看起来跟我做的事很不一样 —— 走的是 D-Bus 和 systemd，而我是裸 Unix socket ——
但形状完全相同，因为这个形状是由上面的判据决定的，不是由口味决定的。
把重进程启动一次，把前言付一次，每开一个窗口发一条消息。

思路可以搬。*管道才是那些有意思的错误住的地方* —— 这个系列的后半段基本都在讲这个。

= 那么账单是多少

我以为这里会是决定性的因素，而且我把价格记成了「零窗口 700 MB 到 1 GB」。
后来发现这两半都是错的。在机器上量出来是这样：

#table(
  columns: (1fr, auto),
  table.header([状态], [内存]),
  [常驻，零窗口，用我真实的 profile], [140 MiB],
  [常驻，开着若干标签页], [约 2 GiB],
)

底价比我以为的小七倍 —— 而且换成真实 profile 几乎没动（空 profile 136 MiB
对真实 profile 140 MiB），所以*你付的也不是 profile 的钱*。

把它推上 GB 的是*使用*：页面 renderer 和它们的 JavaScript 堆会随着浏览不断累积。
那不是常驻的锅 —— 一个不常驻的浏览器开着同样这些标签页，一样要付。常驻设计只多收了那个底价。

（这两个数字怎么量出来的，包括怎么在不打扰你正在用的实例的前提下量，
在《什么该留在 RAM》那一章有一个完整的实验。）

#think[
  底价只有 140 MiB，而不是 1 GB。

  这会怎么改变「值不值得常驻」这个判断？先自己想一想 —— 下面是我的结论，
  它跟我原来的想法不一样。
]

= 频率这个判据，比我以为的弱得多

我原本的推理是：常驻要占内存，所以只有用得足够频繁才划算。

按真实数字算一遍：

- 一天开 20 次：换回约 20 秒/天，代价 140 MiB。
- 一个月开一次：为省 1 秒，一直占着 140 MiB。

在一台*跑得动这个浏览器*的机器上，第二行并不明显是笔坏买卖。140 MiB 在一台
能开 Chromium 的机器上是零头。

于是结论反过来了：*频率几乎不做决定。真正把关的是可分离性。*
把一个程序排除在外的，不是它贵，而是它*没有可拆开的前言可以保温*。

= 没人提的代价

内存是看得见的价格。还有两个安静得多的。

== 常驻进程是「启动那一刻的配置」的快照

此后每一次改配置，都多出第二步 —— 重启它。而忘记这一步，看起来跟「配置里有 bug」
一模一样。你用「启动慢」换来了「静默地过期」，后者是更糟的失败模式。

== 一个启动一次就永不停止的进程，必须是你杀得掉的进程

这件事做砸了，你不会退回一个「启动慢的浏览器」，你会得到一堆攥着内存不放的孤儿
renderer 进程，直到重启。

*这一条是我真正踩进去的坑*，而且它连带出的东西比预想的多得多 —— 包括我一度
把自己的输入法搞没了。它值得单独一章。

= 判断的一条线

#quote(block: true)[
  给「开销恒定且重复」的那部分做常驻。凡是取决于请求的东西，让它该多慢就多慢。
]

对浏览器来说，这条线落在引擎和窗口之间。

= 这门课接下来

- *谁负责收尸* —— 生命周期的那一半。为什么 PID namespace 是错的工具，
  以及我怎么把输入法搞丢的。
- *什么该留在 RAM* —— 这条线具体画在哪里，以及常驻到底占多少内存。
- *快路径用什么写* —— 热路径那一半，五种语言九个实现，实测。
